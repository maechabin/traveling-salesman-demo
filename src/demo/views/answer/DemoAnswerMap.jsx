import React from 'react';
import PropTypes from 'prop-types';

const DemoAnswerMap = (props) => {
  return (
    <div className="DemoAnswerMap">DemoAnswerMap</div>
  );
};

DemoAnswerMap.propTypes = {

};

export default DemoAnswerMap;
